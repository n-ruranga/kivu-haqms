import 'package:flutter/material.dart';
import 'package:kivu_haqms/core/widgets/placeholder_page.dart';

/// Placeholder find-doctor screen — Kethia will implement doctor search & listing.
class FindDoctorPage extends StatelessWidget {
  const FindDoctorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderPage(
      title: 'Doctors',
      subtitle: 'Find a specialist — Kethia',
    );
  }
}
