import 'package:flutter/material.dart';
import 'package:kivu_haqms/core/widgets/placeholder_page.dart';

/// Placeholder queue tracker — Naillah will implement live queue status.
class QueueTrackerPage extends StatelessWidget {
  const QueueTrackerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderPage(
      title: 'Queue Tracker',
      subtitle: 'Live queue status — Naillah',
    );
  }
}
